on: [push]

env:
  TEST: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      # https://github.com/nektos/act#module_not_found
      - uses: actions/checkout@v3
        with:
          path: validate
      - id: validate
        uses: ./validate