on: [push]

jobs:
  slack_notify:
    runs-on: ubuntu-latest
    steps:
      - uses: ./slack_notifier
        if: always()
        with:
          slack-bot-token: 'mytoken'
          slack-channel-id: 'mychannelid'